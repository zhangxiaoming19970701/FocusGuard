# Known Limitations

## 必须在真机验证

本仓库已实现代码层 MVP，但尚未在具体手机上完成说明书要求的三厂商验证和 72 小时稳定性测试。不同厂商的后台限制、窗口实现、游戏引擎、分屏和画中画行为差异较大。

## 普通安装模式边界

- 不能绝对阻止卸载、强制停止、关闭无障碍权限或清除数据。
- 不能绝对阻止用户修改系统时间；同一开机周期的休息倒计时使用单调时钟抵抗调时，但重启后的恢复仍需参考墙钟时间。
- AccessibilityService 通常可快速把目标应用移出前台并显示拦截页，但部分厂商系统窗口、游戏悬浮窗、分屏或 PiP 可能需要机型专项适配。
- Android 的后台启动限制可能导致个别系统版本上全屏 Activity 出现延迟；需用目标设备验证并决定是否增加兼容型前台服务或无障碍覆盖层。
- 全局休息会覆盖普通应用，但电话、紧急呼叫、来电界面、System UI 与 FocusGuard 自身保持可用。

## 当前 MVP 未包含

- 云账户、远程家长端、多设备同步和订阅。
- Device Owner / DPC 防绕过部署。
- 应用分组批量规则。
- CSV/Excel 导出。
- 厂商专用后台自启动页面的逐品牌深链。
- 正式 Google Play 商店素材、审核视频和发布签名。
- 对 UsageStats 历史缺口进行自动写回补偿；当前仅在服务重连时用于前台状态校核，避免其成为实时拦截的唯一信号。

## 发布前不得宣称

在完成 `docs/TEST_PLAN.md` 的真机验收前，不应宣称“无法绕过”“100% 不漏检”“兼容所有 Android 手机”或“额外耗电低于 2%”。
