# Changelog

## 0.1.1 - 2026-08-07

- 修复锁屏后返回原应用时可能没有新无障碍窗口事件、导致禁用规则不再触发的问题。
- 在亮屏、用户解锁和无障碍服务重连后进行有界的活动窗口重判，并以 UsageStats 作为降级信号。
- 修正把 `AccessibilityService.onInterrupt()` 误当作服务断开的生命周期错误。
- 健康检查同时校验权限、进程内连接和持久化心跳，能识别“权限仍开但服务已失联”。
- 开机、首次解锁和应用升级后的健康检查增加启动宽限并使用唯一任务去重。
- 健康检查周期调整为系统允许的 15 分钟最小周期；服务心跳降为每 10 分钟一次。
- 移除无效的锁定启动广播配置，避免在凭据加密存储不可用时启动 WorkManager。

## 0.1.0 - 2026-08-06

- 建立 FocusGuard Android 原生项目。
- 实现连续使用、每日额度、禁用时段、允许时段与跨午夜规则。
- 实现事件驱动前台监测、会话合并、前台秒数累计和全屏拦截。
- 实现自绘 PIN 键盘、带盐哈希、错误锁定、恢复码和管理员临时解除。
- 实现 Room/DataStore 持久化、开机/时间变化恢复、权限健康、统计与审计。
- 增加 GitHub Actions 自动测试和 debug APK 构建。
- 增加规则引擎、时间段和日界线单元测试。
